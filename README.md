# downstream-patches
Patch files made by the SplashOS team that will likely never be made upstream.

Any patches in MVDW-SplashOS/BuildScripts are for patches that came outside the SplashOS ecosystem 

## Contributing
If you forked a repository, please execute the following command to generate a patch file:
```bash
git diff > patch_name.patch
```
