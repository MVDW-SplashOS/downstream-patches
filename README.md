# downstream-patches
Patch files for SplashOS that will likely never be made upstream.

## Contributing
If you forked a repository, please execute the following command to generate a patch file:
```bash
git diff > patch_name.patch
```
